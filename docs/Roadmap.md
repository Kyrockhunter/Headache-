# Fantasy Optimizer Development Roadmap
### Generated: 2025-10-16 02:29:27
---
## Highest Level of importance modules and work needed 
    ! - Recode cleaner module for new data set 
## The following lines will be A-Z and will indicate intended features that are most important before phase x with x being integers. If no Phase(A-Z) is present then PHASE 1 will be the first feature that is intended. After each Phase is completed it will be marked with the String "##[COMPLETED]" Immediately folowing the line that contains Phase(Alphanumeric) - Title. To be clear Phase(Alphanumeric) can be Phase A, Phase M, Phase 1, Phase 3 or any similiar letter or number following Phase. The Title is the string following the "-" in the phase (AN) and is the items title followed by the description of the inteded phase.##

## Phase A.-  Integrate nfl_data_py to obtain stats to use in the decision making process of this program. 

Goal: Streamline statistical collection for simulations and lineup creation.  import functions for play-by-play data, weekly data, seasonal data, rosters, win totals, scoring lines, officials, draft picks, draft pick values, schedules, team descriptive info, combine results and id mappings across various sites.

## Phase B. - Player Reliability scoring.
Goal: Create a score for each player based on how the player did in the previous weeks. If the player outperformed the projections they recieve a higher score. If the player underperformed they recieve a lower score. The algorithm should include considerations for HOME/AWAY, Opponent Ranking against Player Position, etc. For Players that overperform consistently the algorithm can add a predetermined bonus. Scoring is still being developed.

## Phase C. - Lessons Module.
Goal. Track each weeks lineups that are generated by the program and the final results. Use the data from the final results that show the lineups from all contestants. Use the results to find how to tweak the lineups and finish in cash spots. 

## Phase 1 — Advanced Scoring Prediction Overview
Goal: Extend the lineup optimizer beyond static projections to include contextual, correlation-based, and AI‑driven prediction.

**Current (v4.3.x)**: Stable optimizer with FLEX constraint modeling and fallback safety.

### Next steps:
1. Integrate hybrid projection blending.
2. Add correlated player simulation (Monte Carlo).
3. Introduce feature engineering pipeline.
4. Support optional ML models for projection tuning.

---


## Phase 2 — Architecture Plan
```
src/
├── optimizer.py
├── correlated_simulator.py
├── team_simulation.py
├── cleaner.py
├── hybrid_selector.py     ← NEW
├── feature_engineering.py ← NEW
└── model_training.py      ← NEW
```
---

### A. Integrate f
### 1. feature_engineering.py
- Rolling averages, opponent rank, team total, weather, pace.
- Correlation metrics (QB→WR, RB→OL, DEF→RB).

### 2. hybrid_selector.py
- Weighted projection blending across sources.
- Bayesian and correlation adjustments.
- Output: adjusted EV per player.

### 3. correlated_simulator.py
- Monte Carlo simulations with covariance matrix.
- Compute mean, std, p95, boom/bust probabilities.

### 4. model_training.py
- Optional ML model training (XGBoost/CatBoost/Ridge).
- Output: model.joblib for runtime use.

### 5. optimizer.py Integration
- Preprocess using hybrid_selector before LP optimization.

---
## Phase 3 — Simulation & Evaluation Layer
- Run thousands of simulated slates.
- Measure lineup distribution, variance, and stability.
- Rank lineups by ROI and win probability.

---
## Phase 4 — Continuous Improvement
- Weekly accuracy audits.
- Progressive model retraining.
- Streamlit dashboard for trend visualization.
